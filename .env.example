# Reference only: export variables in the shell; .env is not automatically loaded.
APP_ENV=development
# SQLite is the only implemented backend; other values fail validation.
DB_BACKEND=sqlite
DB_NAME=produtos.db
SQLITE_TIMEOUT=5
API_DOCS_ENABLED=true
